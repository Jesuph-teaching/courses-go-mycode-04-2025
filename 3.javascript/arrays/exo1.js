const songs = ['Shape of you ', 'Blinding light', 'Levitating'];

songs.push('Tv off');

console.log(songs);

/* const lastSong = songs.pop();
console.log(lastSong); */

console.log(songs.pop());

console.log(songs);

songs.unshift('Not lik us');

console.log(songs);

console.log(songs.shift());

console.log(songs);
