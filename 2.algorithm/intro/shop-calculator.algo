ALGORITHM shop_calculator
VAR
    price:FLOAT;
    quantity:INTEGER;
    sum:FLOAT;
    current_product:INTEGER;
BEGIN
    // allowed to enter 5 product
    current_product:=1;
    sum:=0;
    WHILE(current_product<=5)DO
        // ask for product price ( read )
        // ask for quantity( read )
        // add to sum
        current_product:=current_product+1;
    END_WHILE
    // display total price; ( write )
END